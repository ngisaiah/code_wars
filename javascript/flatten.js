// Desc: Write a function that flattens an Array of Array objects into a flat Array. Your function must only do one level of flattening.

// Solution:

function flatten(array) {
  // flat method will flatten 1 level of your array
    return array.flat();
}
